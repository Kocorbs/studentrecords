# Database Configuration
# For local development, use a standard PostgreSQL connection string
# For production (Vercel), use Prisma Accelerate connection string
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_PRISMA_ACCELERATE_API_KEY"

# Instructions:
# 1. Copy this file to .env
# 2. Replace YOUR_PRISMA_ACCELERATE_API_KEY with your actual Prisma Accelerate API key
# 3. For Vercel deployment, add DATABASE_URL to your project's environment variables
#    (Settings → Environment Variables in Vercel dashboard)
